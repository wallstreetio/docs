[project]
name = "docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.43",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pre-commit-uv",
    "ruff",

]